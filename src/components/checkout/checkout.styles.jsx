import styled from 'styled-components';

export const CheckoutContainer = styled.div`
    width: auto;
    min-height: 10vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    margin: 50px auto 0;
    padding: 5px 10px;
    `;

export const CheckoutTitleContainer = styled.h2`

`;

export const CheckoutButtonsContainer = styled.div`
    display: flex;
    flex-direction: row;
    padding: 5px;
`;
